# Core dependencies
streamlit
pandas
numpy
requests

# Technical indicators (choose one below)
ta                         # Pure Python (easy to install, slower)
# TA-Lib                   # Uncomment ONLY if installed properly (faster but harder to install)

# Real-time WebSocket support
websocket-client

# Auto-refresh support
streamlit-autorefresh

# Optional: For faster pandas (if used)
# modin[all]               # Only use if you're intentionally using Modin

# Optional: If using concurrency/threading/multiprocessing
# no extra install needed (standard library)
